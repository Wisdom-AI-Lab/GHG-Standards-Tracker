import {escapeHtml as e,formatDate,safeUrl} from './core.mjs';
import {PUBLIC_SOURCE,PUBLIC_ENTITIES,SCENARIOS,GUIDED_QUESTIONS,RESULTS,BOUNDARIES,snapshot,isMember,boundaryPosition,assessmentRows,visibleRows,scenarioDiff,demoSources,answerQuestion} from './demo-core.mjs';

const button=(attribute,value,label,active=false)=>`<button type="button" class="demo-pill ${active?'selected':''}" ${attribute}="${e(value)}" aria-pressed="${active}">${e(label)}</button>`;
const resultBadge=status=>`<span class="badge demo-result ${e(status)}">${e(RESULTS[status])}</span>`;
const known=value=>value==null?'Unknown':typeof value==='boolean'?(value?'Yes':'No'):String(value);
const link=(url,label)=>safeUrl(url)?`<a href="${e(safeUrl(url))}" target="_blank" rel="noopener noreferrer">${e(label)} ↗</a>`:'';

function contextControls(state,compact=false) {
  return `<div class="demo-context"><div><p class="eyebrow">CHOOSE THE EVIDENCE CONTEXT</p><div class="demo-switch">${button('data-demo-profile','fictional','Atlas · fictional scenario',state.profile==='fictional')}${button('data-demo-profile','public','PepsiCo · public extract',state.profile==='public')}</div></div>${compact?'<a href="#workspace">Open client workspace →</a>':''}</div>`;
}
function scenarioControls(state) {
  if(state.profile==='public')return `<div class="public-note"><strong>Five selected public subsidiaries · observed ${e(formatDate(PUBLIC_SOURCE.as_of))}</strong><p>Links mean “listed subsidiary in Exhibit 21,” not direct ownership. No control, financial or legal-applicability facts are inferred. Business simulations are disabled for this example.</p><button type="button" class="text-button" data-demo-source="public-entities">Inspect source and limits ↗</button></div>`;
  return `<div class="scenario-toolbar"><div class="scenario-buttons"><span class="eyebrow">SIMULATE A CHANGE</span><div class="demo-switch">${Object.entries(SCENARIOS).map(([id,s])=>button('data-demo-scenario',id,s.label,state.scenario===id)).join('')}</div></div><label>As-of date<select id="demo-date"><option value="2026-08-27" ${state.asOf==='2026-08-27'?'selected':''}>27 Aug 2026 · before event</option><option value="2027-02-01" ${state.asOf==='2027-02-01'?'selected':''}>1 Feb 2027 · after event</option></select></label><label>GHG boundary method<select id="demo-boundary">${Object.entries(BOUNDARIES).map(([id,label])=>`<option value="${id}" ${state.boundary===id?'selected':''}>${e(label)}</option>`).join('')}</select></label></div><div class="scenario-description"><span class="badge pending">FICTIONAL INPUTS & RULES</span><span>${e(SCENARIOS[state.scenario].note)} ${state.scenario!=='baseline'?`Event ${snapshot(state).applied?'applied':'not yet effective'} at selected date.`:''}</span></div>`;
}
function stats(state) {
  const rows=assessmentRows(state), snap=state.profile==='fictional'?snapshot(state):null;
  const members=snap?snap.entities.filter(x=>x.type==='entity'&&isMember(x,snap.entities)).length:PUBLIC_ENTITIES.length;
  return `<div class="metrics demo-metrics"><div class="metric"><strong>${members}</strong><span>${snap?'Member legal entities (fictional)':'Selected subsidiary records'}</span></div><div class="metric"><strong>${rows.filter(r=>r.status==='match').length}</strong><span>Illustrative trigger matches · not legal findings</span></div><div class="metric warn"><strong>${rows.filter(r=>r.status==='unknown').length}</strong><span>Unresolved assessment rows</span></div><div class="metric"><strong>${scenarioDiff(state).length}</strong><span>Rows changed from baseline</span></div></div>`;
}
function entityNode(entity,state,entities,isPublic=false) {
  const member=isPublic||isMember(entity,entities);
  return `<button type="button" class="entity-node ${state.entity===entity.id?'selected':''} ${member?'':'outside'}" data-demo-entity="${e(entity.id)}" aria-pressed="${state.entity===entity.id}"><span>${e(isPublic?'Listed subsidiary':entity.type==='facility'?'Facility':'Fictional legal entity')}</span><strong>${e(entity.name)}</strong><small>${e(entity.jurisdiction)}</small><em>${e(isPublic?'Direct parent / control unknown':member?boundaryPosition(entity,entities,state.boundary):'Outside group at selected date')}</em></button>`;
}
function graph(state) {
  const isPublic=state.profile==='public', snap=isPublic?null:snapshot(state);
  const entities=isPublic?PUBLIC_ENTITIES:snap.entities;
  const roots=isPublic?entities:entities.filter(x=>x.parent==='atlas');
  return `<section class="demo-panel"><div class="section-heading"><div><p class="eyebrow">SELECT A NODE TO INSPECT ITS FACTS</p><h2>${isPublic?'Public subsidiary evidence':'Organisation graph · scenario projection'}</h2></div>${button('data-demo-entity','all','Clear selection',state.entity==='all')}</div><div class="org-graph"><div class="graph-root"><span>${isPublic?'FILING GROUP — NOT A DIRECT-PARENT ASSERTION':'FICTIONAL REPORTING GROUP'}</span><strong>${isPublic?'PepsiCo, Inc.':'Atlas Beverages Group'}</strong><small>${isPublic?'Selected Exhibit 21 relationships only':e(BOUNDARIES[state.boundary])+' · '+e(formatDate(state.asOf))}</small></div><p class="edge-legend">${isPublic?'Dashed links: listed subsidiary; intermediate entities not established':'Solid links: fictional parent/reporting relationship · Boundary result uses separate control facts'}</p><ul class="org-branches ${isPublic?'public-branches':''}">${roots.map(entity=>`<li>${entityNode(entity,state,entities,isPublic)}${!isPublic&&entities.some(x=>x.parent===entity.id)?`<ul class="facility-branch">${entities.filter(x=>x.parent===entity.id).map(child=>`<li>${entityNode(child,state,entities)}</li>`).join('')}</ul>`:''}</li>`).join('')}</ul></div></section>`;
}
function inspector(state) {
  const isPublic=state.profile==='public';const snap=isPublic?null:snapshot(state);
  const entity=(isPublic?PUBLIC_ENTITIES:snap.entities).find(x=>x.id===state.entity);
  if(!entity)return `<aside class="demo-panel inspector"><p class="eyebrow">ENTITY INSPECTOR</p><h2>From relationship to evidence</h2><p>Select a graph node to see what is known, what is assumed and what is missing.</p><div class="check-note">${isPublic?'Jurisdiction is a starting point for research, not proof of applicability.':'The fixture deliberately separates equity ownership, financial control and operational control. Change the boundary method to see the difference.'}</div><p>Open <strong>Applicability</strong> for the jurisdiction × entity × instrument × trigger matrix.</p></aside>`;
  const facts=isPublic?[['Jurisdiction',entity.jurisdiction],['Direct parent','Unknown'],['Ownership / control','Unknown'],['Listing / revenue','Unknown'],['Observed on',formatDate(PUBLIC_SOURCE.as_of)],['Source location',entity.locator]]:[['Jurisdiction',entity.jurisdiction],['Parent / reports to',snap.entities.find(x=>x.id===entity.parent)?.name||'Group root'],['Ownership',entity.ownership+'% (fictional)'],['Financial control',known(entity.financial_control)],['Operational control',known(entity.operational_control)],['Revenue',entity.revenue_musd==null?'Unknown':`USD ${entity.revenue_musd}m (fictional)`],['Listed',known(entity.listed)],['UK nexus',known(entity.uk_nexus)],['Boundary position',boundaryPosition(entity,snap.entities,state.boundary)]];
  return `<aside class="demo-panel inspector"><p class="eyebrow">${isPublic?'PUBLIC FACTS — PARTIAL':'FICTIONAL FACTS — SCENARIO'}</p><h2>${e(entity.name)}</h2><dl>${facts.map(([k,v])=>`<div><dt>${e(k)}</dt><dd>${e(v)}</dd></div>`).join('')}</dl><button type="button" class="text-button" data-demo-source="${isPublic?'public-entities':'fictional-facts'}">Inspect evidence ↗</button><p class="inspector-note">${isPublic?'No actual client relationship or endorsement is implied.':'A minority investment may have different outcomes under different boundary methods. Control here is an explicit scenario assumption, not inferred from percentage.'}</p></aside>`;
}
export function matrixTable(rows,{compact=false,changes=false}={}) {
  if(!rows.length)return '<div class="empty"><h2>No assessment rows in this selection</h2><p>A facility is not assessed as a separate legal entity in this fixture. Clear filters or choose a subsidiary.</p></div>';
  return `<div class="table-wrap demo-table"><table><caption class="sr-only">Jurisdiction by entity, instrument, trigger and demonstration result</caption><thead><tr><th scope="col">Jurisdiction / entity</th><th scope="col">Instrument / standard</th>${compact?'':'<th scope="col">Trigger / period</th>'}${changes?'<th scope="col">Baseline</th>':''}<th scope="col">${changes?'Scenario result':'Assessment'}</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${e(r.jurisdiction)}</strong><small>${e(r.entity)}</small></td><td>${e(r.rule)}<small>${e(r.standard)}</small></td>${compact?'':`<td>${e(r.trigger)}<small>Period: ${e(r.period)} · ${e(r.version)}</small></td>`}${changes?`<td>${resultBadge(r.before)}</td>`:''}<td>${resultBadge(r.status)}<small><button class="text-button" type="button" data-demo-assessment="${e(r.id)}">Why this result? ↗</button></small></td></tr>`).join('')}</tbody></table></div>`;
}
function matrix(state) {
  const entities=state.profile==='public'?PUBLIC_ENTITIES:snapshot(state).entities.filter(x=>x.type==='entity');
  const rows=visibleRows(state);const jurisdictions=[...new Set(assessmentRows(state).map(r=>r.jurisdiction))];
  return `<section><div class="section-heading"><div><p class="eyebrow">JURISDICTION × ENTITY × INSTRUMENT × TRIGGER</p><h2>Applicability demonstration</h2></div></div><div class="matrix-filters"><label>Entity<select id="demo-entity"><option value="all">All entities</option>${entities.map(x=>`<option value="${e(x.id)}" ${state.entity===x.id?'selected':''}>${e(x.name)}</option>`).join('')}</select></label><label>Instrument jurisdiction<select id="demo-jurisdiction"><option value="all">All jurisdictions</option>${jurisdictions.map(j=>`<option ${state.jurisdiction===j?'selected':''}>${e(j)}</option>`).join('')}</select></label><button type="button" class="button quiet" data-demo-clear-filters>Clear table filters</button></div><p class="demo-small">${rows.length} rows displayed. ${state.profile==='public'?'No actual local adoption rule has been assessed.':'All three invented rules are tested against each legal entity, including cross-border nexus. GHG boundary selection does not change legal trigger results.'}</p>${matrixTable(rows)}</section>`;
}
function changes(state) {
  if(state.profile==='public')return '<section class="demo-panel"><h2>Public history is not inferred</h2><p>The exhibit provides a dated snapshot, not a transaction history. Switch to Atlas to explore hypothetical changes.</p></section>';
  const snap=snapshot(state), baseline=snapshot({...state,scenario:'baseline'});
  const changed=snap.entities.map(entity=>({entity,before:boundaryPosition(baseline.entities.find(x=>x.id===entity.id),baseline.entities,state.boundary),after:boundaryPosition(entity,snap.entities,state.boundary)})).filter(x=>x.before!==x.after);
  const diff=scenarioDiff(state);
  return `<section class="demo-panel"><p class="eyebrow">BASELINE → SELECTED SCENARIO</p><h2>${e(SCENARIOS[state.scenario].label)} · ${e(formatDate(state.asOf))}</h2><p>${e(SCENARIOS[state.scenario].note)}</p><p>Effective date: ${SCENARIOS[state.scenario].effective_on?e(formatDate(SCENARIOS[state.scenario].effective_on)):'No event'} · Version: ${e(snap.version)}. Previous snapshots remain unchanged.</p><h3>Boundary changes — ${e(BOUNDARIES[state.boundary])}</h3>${changed.length?`<ul class="change-list">${changed.map(x=>`<li><strong>${e(x.entity.name)}</strong><span>${e(x.before)} → ${e(x.after)}</span></li>`).join('')}</ul>`:'<p>No boundary changes at the selected date and method.</p>'}<h3>Demonstration assessment changes</h3>${diff.length?matrixTable(diff,{changes:true}):'<p>No assessment status changes at the selected date. Choose an event and the after-event date to demonstrate its effect.</p>'}<div class="check-note">This is not an audit log or a production versioned graph. Scenarios are independent, in-memory overlays. No approval is recorded and no client data is modified.</div></section>`;
}
export function renderWorkspace(state) {
  return `<div class="demo-banner"><strong>INTERACTIVE CONCEPT DEMO</strong><span>Public evidence and invented scenarios are kept separate. No real-client applicability conclusions.</span></div>${contextControls(state)}${scenarioControls(state)}${stats(state)}<div class="workspace-tabs" role="group" aria-label="Client workspace section">${button('data-demo-tab','graph','Organisation graph',state.tab==='graph')}${button('data-demo-tab','matrix','Applicability',state.tab==='matrix')}${button('data-demo-tab','changes','Changes & history',state.tab==='changes')}<a href="#notebook">Ask in the notebook →</a></div>${state.tab==='matrix'?matrix(state):state.tab==='changes'?changes(state):`<div class="workspace-grid">${graph(state)}${inspector(state)}</div>`}`;
}
export function renderAssessment(id,state) {
  const row=assessmentRows(state).find(r=>r.id===id);
  if(!row)return '<h2 id="dialog-title">Assessment unavailable</h2><p>The active scenario or profile has changed.</p>';
  return `<p class="eyebrow">${state.profile==='public'?'PUBLIC EVIDENCE — UNRESOLVED':'INVENTED RULE — NOT LEGAL ADVICE'}</p><h2 id="dialog-title">${e(row.entity)}</h2><p>${e(row.jurisdiction)} · ${e(row.rule)} · ${e(row.standard)}</p>${resultBadge(row.status)}<p style="margin-top:14px">${e(row.note)}</p>${row.trace.length?`<h3>Condition trace — all conditions required</h3><dl class="trace-list">${row.trace.map(t=>`<div><dt>${e(t.label)}</dt><dd>Actual: ${e(known(t.actual))} · ${t.pass===null?'UNKNOWN':t.pass?'PASS':'FAIL'}</dd></div>`).join('')}</dl>`:'<p>Only the name, listed-subsidiary relationship and jurisdiction are supported by the selected public exhibit. The applicable instrument, listing, size, nexus and exemptions require research.</p>'}<p class="provenance">${e(row.version)} · Period: ${e(row.period)}. A match in a fixture is not an approved compliance determination. No exemptions or assurance findings are modelled.</p>`;
}
export function renderDemoSource(id,data) {
  const source=demoSources(data).find(s=>s.id===id);
  if(!source)return '<h2 id="dialog-title">Source unavailable</h2>';
  return `<p class="eyebrow">${e(source.kind)}</p><h2 id="dialog-title">${e(source.title)}</h2><p>${e(source.text)}</p>${source.id==='public-entities'?`<p>Observed ${e(formatDate(source.as_of))}; source checked ${e(formatDate(source.checked_on))}. Selected rows:</p><ul>${PUBLIC_ENTITIES.map(x=>`<li>${e(x.name)} — ${e(x.jurisdiction)} (${e(x.locator)})</li>`).join('')}</ul>`:''}${source.url?link(source.url,'Open original source'):'<p class="check-note">Locally authored sample content. Not a regulatory instrument or a real client document.</p>'}`;
}
function answerHtml(answer) {
  return `<article class="notebook-answer" aria-live="polite"><p class="eyebrow">${answer.supported?'GUIDED RESPONSE · NOT MODEL-GENERATED':'DEMO LIMITATION'}</p><h3>${e(answer.title)}</h3><p>${e(answer.text)}</p>${answer.context?`<p class="answer-context">Context: ${e(answer.context)}</p>`:''}${answer.rows?.length?matrixTable(answer.rows,{compact:true,changes:answer.questionId==='changes'}):''}${answer.citations.length?`<div class="citations"><span>Supporting notes:</span>${answer.citations.map((id,i)=>`<button type="button" class="text-button" data-demo-source="${e(id)}">[${i+1}] ${e(id)}</button>`).join('')}</div><button type="button" class="button quiet" data-demo-save>Save response in this session</button>`:''}</article>`;
}
export function renderNotebook(state,data) {
  const sources=demoSources(data);const answer=state.answer?answerQuestion(state.answer,state,data):null;
  return `<div class="demo-banner"><strong>GUIDED DEMO — NO LIVE AI</strong><span>Five supported questions · no file uploads · no external model requests · refresh clears session notes</span></div>${contextControls(state,true)}<div class="notebook-context"><strong>Current context:</strong> ${state.profile==='public'?'PepsiCo public extract · 27 Dec 2025':`Atlas · ${e(SCENARIOS[state.scenario].label)} · ${e(formatDate(state.asOf))} · ${e(BOUNDARIES[state.boundary])}`}. Answers are recalculated from this context; table filters do not restrict them.</div><div class="notebook-grid"><aside class="notebook-panel source-panel"><p class="eyebrow">01 / SOURCES</p><h2>Choose the evidence</h2><p>Unselect a required source to see the answer withheld.</p>${sources.map(s=>`<div class="notebook-source"><label><input type="checkbox" data-demo-source-toggle="${e(s.id)}" ${state.sources.includes(s.id)?'checked':''}><span><strong>${e(s.title)}</strong><small>${e(s.kind)}</small></span></label><button class="text-button" type="button" data-demo-source="${e(s.id)}">Read source note ↗</button></div>`).join('')}<button type="button" class="button quiet" disabled>Add a client file · planned</button><p class="demo-small">No file picker or ingestion is active. Use only these bundled public and fictional sources.</p></aside><section class="notebook-panel chat-panel"><p class="eyebrow">02 / QUESTIONS</p><h2>Ask about the client context</h2><div class="question-options">${GUIDED_QUESTIONS.map(q=>`<button type="button" class="question-chip" data-demo-question="${e(q.id)}">${e(q.question)}</button>`).join('')}</div>${answer?answerHtml(answer):'<div class="notebook-welcome"><strong>Start with a guided question</strong><p>Explore a matrix, inspect missing facts, or explain a scenario. Source notes will accompany supported answers.</p></div>'}<form id="demo-question-form"><label for="demo-question">Try one of the example questions</label><div class="question-input"><input id="demo-question" name="question" type="text" maxlength="500" value="${e(state.question)}" placeholder="Which jurisdictions and triggers apply?" autocomplete="off"><button type="submit" class="button">Ask</button></div><small>Exact example questions and a few aliases are supported. Other questions receive a limitation message.</small></form></section><aside class="notebook-panel output-panel"><p class="eyebrow">03 / OUTPUTS</p><h2>Session notes</h2><p>Responses saved for this walkthrough only; they are not uploaded or retained after refresh.</p>${state.saved.length?state.saved.map((note,i)=>`<article class="saved-note"><span>NOTE ${i+1}</span><h3>${e(note.title)}</h3><p>${e(note.text)}</p><small>${e(note.context)}</small><p class="demo-small">Evidence: ${e(note.citations.join(', '))} · Saved snapshot, not updated with later scenario changes.</p></article>`).join(''):'<div class="output-empty">Save a supported response to demonstrate a review handoff.</div>'}<button class="button quiet" type="button" data-demo-clear-notes ${state.saved.length?'':'disabled'}>Clear session notes</button><p class="demo-small" role="status">${e(state.notice)}</p><div class="future-output"><strong>Full build provisions</strong><span>Private source uploads</span><span>Model-backed cited answers</span><span>Reviewed guidance exports</span><span>Client-specific access controls</span></div></aside></div>`;
}
